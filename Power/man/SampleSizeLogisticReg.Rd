% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Logistic.R
\name{SampleSizeLogisticReg}
\alias{SampleSizeLogisticReg}
\title{Sample Size Calculation (Logistic Regression)}
\usage{
SampleSizeLogisticReg(
  beta_0,
  beta_x,
  beta_z,
  var_x,
  var_z,
  cov_xz,
  max_n,
  power = 0.8,
  t1e = 0.05,
  min_n = 1,
  tol = .Machine$double.eps^0.25,
  method = c("auto", "quadrature", "montecarlo"),
  n_quad = 15L,
  n_sim = 100000L
)
}
\arguments{
\item{beta_0}{Intercept (scalar).}

\item{beta_x}{True regression coefficient for X (vector or matrix).}

\item{beta_z}{True regression coefficient for Z (vector or matrix).}

\item{var_x}{Variance of X (matrix).}

\item{var_z}{Variance of Z (matrix).}

\item{cov_xz}{Covariance of X and Z (matrix).}

\item{max_n}{Maximum allowable sample size (upper bound for search).}

\item{power}{Target power.}

\item{t1e}{Type I error level.}

\item{min_n}{Minimum allowable sample size (lower bound for search).}

\item{tol}{Numeric tolerance for \code{\link[stats]{uniroot}}.}

\item{method}{\code{"auto"}, \code{"quadrature"}, or \code{"montecarlo"}.}

\item{n_quad}{Number of quadrature points per dimension (quadrature only).}

\item{n_sim}{Number of Monte Carlo draws (Monte Carlo only).}
}
\value{
A data frame with columns \code{n} (sample size) and \code{power}
(achieved power at that \code{n}).
}
\description{
Sample size for testing \eqn{H_0: \beta_X = 0} in the logistic regression
model \eqn{\mathrm{logit}(\pi) = \beta_0 + X'\beta_X + Z'\beta_Z}. Finds the
smallest integer \eqn{n} in \eqn{[\code{min_n}, \code{max_n}]} such that
power reaches the target. The expected information is approximated once
(by quadrature or Monte Carlo) and reused in the root-finding search.
}
\details{
Uses \code{\link[stats]{uniroot}} to find the smallest sample size
at which power reaches the target. The efficient information is computed
once and held fixed during the search.
}
\seealso{
\code{\link{PowerLogisticReg}} for power at a given sample size.
}
