% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Logistic.R
\name{PowerLogisticReg}
\alias{PowerLogisticReg}
\title{Power Calculation (Logistic Regression)}
\usage{
PowerLogisticReg(
  beta_0,
  beta_x,
  beta_z,
  var_x,
  var_z,
  cov_xz,
  n,
  t1e = 0.05,
  method = c("auto", "quadrature", "montecarlo"),
  n_quad = 15L,
  n_sim = 100000L
)
}
\arguments{
\item{beta_0}{Intercept (scalar).}

\item{beta_x}{True regression coefficient for X (vector or matrix).}

\item{beta_z}{True regression coefficient for Z (vector or matrix).}

\item{var_x}{Variance of X (matrix).}

\item{var_z}{Variance of Z (matrix).}

\item{cov_xz}{Covariance of X and Z (matrix).}

\item{n}{Sample size.}

\item{t1e}{Type I error level.}

\item{method}{\code{"auto"} (quadrature when \eqn{\dim(X)+\dim(Z) \leq 5},
else Monte Carlo), \code{"quadrature"}, or \code{"montecarlo"}.}

\item{n_quad}{Number of quadrature points per dimension (quadrature only).}

\item{n_sim}{Number of Monte Carlo draws (Monte Carlo only).}
}
\value{
Numeric power.
}
\description{
Power for testing \eqn{H_0: \beta_X = 0} in the logistic regression model
\eqn{\mathrm{logit}(\pi) = \beta_0 + X'\beta_X + Z'\beta_Z}, where
\eqn{\pi = P(Y=1 \mid X, Z)}. The distribution of \eqn{(X, Z)} is assumed
multivariate normal with mean zero and covariance given by \code{var_x},
\code{var_z}, and \code{cov_xz}. The expected information
\eqn{E[W W' \pi(1-\pi)]} can be approximated by Gauss-Hermite quadrature
(default for small dimension) or Monte Carlo.
}
\details{
Power is computed using the non-central chi-squared distribution
under the asymptotic Wald test. With \code{method = "quadrature"},
the expectation is approximated by product Gauss-Hermite quadrature
over the normal distribution of \eqn{(X, Z)}, which is typically more
accurate and faster than Monte Carlo when the dimension is small.
}
\seealso{
\code{\link{SampleSizeLogisticReg}} for sample size given a target
power.
}
